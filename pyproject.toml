[project]
name = "minipac-rl"
version = "0.1.0"
description = "Simplified Pac-Man reinforcement learning (value iteration & Monte Carlo)"
requires-python = ">=3.11,<3.12"
dependencies = [
    "numpy>=1.24",
    "pillow>=10.0",
    "matplotlib>=3.7",
]

[tool.uv]
package = false
